#include"bits/stdc++.h"
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n'

int main(){
    optimize();

    char ch = 'v';
    ch = toupper (ch);
    cout << ch << endl;//V
    ch = tolower(ch);
    cout << ch << endl;//v
}
